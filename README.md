# This is a React Skeleton using Vite
Packages installeds:
* React Router v6
* TypeScript

# How to use

`npm install`

`npm run dev` You can now play with the local develop envirenment.

`npm run build` The app are built and served.


---